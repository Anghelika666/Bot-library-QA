# В google colab добавить: !pip install pyTelegramBotAPI
# Чтобы добавить новое слово — нужно его прописать в объект DEFINITOINS на 13 строчке
# Важно все новые аббривиатуры в коде писать только с маленьких букв
# Пользователь в телеграм может писать и с большой и с маленькой — код всегда приводит к строчным

from telebot import TeleBot, types

bot = TeleBot(token='вставьте токен', parse_mode='html') # создание бота

# словарь с определениями и аббревиатурами, которые знает бот
# в формате:
# 'ключевая фраза': 'соответствующее ей определение'
DEFINITOINS = {
    'регресс': 'Вид тестирования, при котором мы проверяем, что новый функционал не сломал существующий',
    'смоук': 'выжимка самых критичных тестов',
    'rps': 'Request per second. запросы в секунду',
    'тз': 'Техническое задание',
    'e2e': 'End-to-end',
    'beta': 'Внешнее тестирование',
    'alfa': 'Внутреннее тестирование',
    'qa': 'Обеспечение качества — процесс контроля и улучшения качества ПО на всех этапах разработки',
    'qc': 'Контроль качества — проверка готового продукта на соответствие требованиям',
    'sdlc': 'Жизненный цикл разработки ПО — этапы от идеи до поддержки продукта',
    'stlc': 'Жизненный цикл тестирования ПО — этапы тестирования от планирования до закрытия',
    'uat': 'Приемочное тестирование — проверка продукта конечными пользователями перед релизом',
    'api': 'Интерфейс для взаимодействия между компонентами ПО (например, фронтенд и бэкенд)',
    'ui': 'Графический интерфейс пользователя — видимая часть приложения',
    'ux': 'Опыт пользователя — удобство и эмоции при взаимодействии с продуктом',
    'sql': 'Язык запросов к базам данных',
    'jira': 'Инструмент для отслеживания багов и управления задачами',
    'баг репорт': 'Отчет о дефекте с описанием шагов воспроизведения и ожидаемого результата',
    'istqb': 'International Software Testing Qualifications Board. Международная организация, сертифицирующая тестировщиков',
    'devops': 'Методология, объединяющая разработку и эксплуатацию для ускорения выпуска ПО',
    'black box': 'Тестирование без доступа к коду',
    'white box': 'Тестирование с доступом к коду',
    'json': 'Формат обмена данными между сервером и клиентом',
    'http': 'Протоколы передачи данных в вебе',
    'https': 'Протоколы передачи данных в вебе(HTTPS — защищенная версия)',
    'nda': 'Соглашение о неразглашении конфиденциальной информации',
    'roi': 'Окупаемость инвестиций — оценка эффективности тестирования',
    'cd': 'Continuous Development. Непрерывная разработка',
    'xp': 'Экстремальное программирование',
    'валидация': 'Соответствия разрабатываемого ПО ожиданиям и потребностям пользователя, требованиям к системе',
    'билд': 'Объединение отдельных модулей программы в одну работающую систему',
    'спецификация ': 'Детальное описание того, как должно работать ПО',
    }


# обработчик команды '/start'
@bot.message_handler(commands=['start'])
def start_command_handler(message: types.Message):
    # отправляем ответ на команду '/start'
    bot.send_message(
        chat_id=message.chat.id, # id чата, в который необходимо направить сообщение
        text='Привет! Я помогу тебе расшифровать сложные аббревиатуры и термины 🤓\nВведи интересующий термин, например, регресс', # текст сообщения
    )

# обработчик всех остальных сообщений
@bot.message_handler()
def message_handler(message: types.Message):
    # пробуем найти ключевую фразу в словаре
    definition = DEFINITOINS.get(
        message.text.lower(), # приводим текст сообщения к нижнему регистру
    )
    # если фразы нет в словаре, то переменная definition будет иметь значение None
    # проверяем это условие
    if definition is None:
        # если ключевая фраза не была найдена в словаре
        # отправляем ответ
        bot.send_message(
            chat_id=message.chat.id,
            text='😋 Я пока не знаю такого определения',
        )
        # выходим из функции
        return
    
    # если ключевая фраза была найдена, формируем текст сообщения и отправляем его
    # если перед строкой поставить букву f, то в фигурных скобках {} можно использовать переменные :)
    bot.send_message(
        chat_id=message.chat.id,
        text=f'Определение:\n<code>{definition}</code>',
    )

    bot.send_message(
        chat_id=message.chat.id,
        text=f'Жду следующий термин',
    )


# главная функция программы
def main():
    # запускаем нашего бота
    bot.infinity_polling()


if __name__ == '__main__':
    main()


# обработчик всех остальных сообщений
@bot.message_handler()
def message_handler(message: types.Message):
    # пробуем найти ключевую фразу в словаре
    definition = DEFINITOINS.get(
        message.text.lower(), # приводим текст сообщения к нижнему регистру
    )
    # если фразы нет в словаре, то переменная definition будет иметь значение None
    # проверяем это условие
    if definition is None:
        # если ключевая фраза не была найдена в словаре
        # отправляем ответ
        bot.send_message(
            chat_id=message.chat.id,
            text='😋 Я пока не знаю такого определения',
        )
        # выходим из функции
        return
    
    # если ключевая фраза была найдена, формируем текст сообщения и отправляем его
    # если перед строкой поставить букву f, то в фигурных скобках {} можно использовать переменные :)
    bot.send_message(
        chat_id=message.chat.id,
        text=f'Определение:\n<code>{definition}</code>',
    )

    bot.send_message(
        chat_id=message.chat.id,
        text=f'Жду следующий термин',
    )


# главная функция программы
def main():
    # запускаем нашего бота
    bot.infinity_polling()


if __name__ == '__main__':
    main()